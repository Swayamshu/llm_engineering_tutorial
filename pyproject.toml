[project]
name = "tutorials"
version = "0.1.0"
description = "This project is built to learn LLM Engineering. This repo provides various tutorials for the same."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "gradio>=6.26.0",
    "httpx>=0.28.1",
    "openai>=3.3.1",
    "python-dotenv>=1.2.3",
]

[tool.uv]
package = false
